import React from "react";

const Chart = ({ type, data }) => {
  // Implement logic to render round or column chart based on type and data

  return <div className="Chart">{/* Render chart here */}</div>;
};

export default Chart;
